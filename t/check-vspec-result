#!/bin/bash

dep_dirs=("$PWD")

diff="$(diff --unified "$2" <(./bin/vspec "${dep_dirs[@]}" "$1"))"
if [ $? = 0 ]
then
  echo 'ok 1'
else
  echo 'not ok 1'
  echo "$diff" | sed 's/^/# /'
fi
echo '1..1'

# __END__
